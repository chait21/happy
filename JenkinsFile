pipeline{
    agent any

    stages{
        stage("SCM"){
            steps{
                git" "
            }
        }
       stage("Build"){
            steps{
                sh " mvn clean package "
            }
        }
    }
}